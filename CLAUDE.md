# CLAUDE.md

Instructions for Claude Code when working in the `novel-translator` repo. Read
`SPEC.md` first before implementing anything — it's the source of truth for the
data model, features, and build order. Then check `TASKS.md` for the current
checklist of what to build next, in order.

Related docs:
- `TRANSLATION_RULES.md` — the actual translation prompt/rules (load this at
  runtime, don't hardcode a prompt in source).
- `GLOSSARY.md` — per-book terminology consistency, explains the `GlossaryTerm`
  model and how it's injected into the prompt.
- `TASKS.md` — step-by-step implementation checklist, phase by phase.
- `AGENTS.md` — generated by `create-next-app`: this Next.js version has breaking
  changes vs. older docs, read `node_modules/next/dist/docs/` before writing
  Next-specific code.

Prisma 7 note: the datasource URL lives in `prisma.config.ts` (which loads
`.env.local`), **not** in `schema.prisma`, and the client needs the
`@prisma/adapter-better-sqlite3` driver adapter. See `.agents/skills/prisma-*`
for the version-accurate API.

## Project Context

A personal app (single-user, runs on a local machine) for reading English novels
with AI-generated Indonesian translations produced in batches, with automatic
progress tracking. See `README.md` for a short overview, `SPEC.md` for technical
detail.

## Working Principles

- **Follow the MVP order in SPEC.md §7 first.** Don't jump ahead to Phase 2
  features (epub/pdf parsers, side-by-side view, etc.) before the basic
  txt + translate + reader flow works end-to-end.
- **`orderIndex` is the source of truth** for paragraph order and progress. Don't
  use text-matching/fuzzy search to determine position — that's exactly the
  problem this app is meant to eliminate from the old manual workflow.
- This is a small, personal-scale project (1 user, local data). Don't over-engineer:
  - No auth/session management needed.
  - No multi-tenancy or row-level security needed.
  - SQLite is enough; don't introduce Postgres/Docker unless explicitly asked.
- Keep API keys and sensitive config in `.env.local`, never hardcoded.

## Tech Stack

- Next.js App Router + TypeScript
- Prisma + SQLite
- Tailwind CSS
- Anthropic API (`@anthropic-ai/sdk`) for translation

## Code Structure

- `src/lib/parser/` — one file per format (`txt.ts`, `epub.ts`, `pdf.ts`), all
  exporting a function with the same signature: `parse(fileBuffer) => Paragraph[]`.
- `src/lib/translator/` — provider-agnostic interface (`types.ts`) + one client
  per provider (`claudeClient.ts`, later `geminiClient.ts`), shared prompt
  builder (reads `TRANSLATION_RULES.md` + glossary terms), and response parser
  (splits the AI's output back into an array of paragraphs based on the
  separator). For MVP, only `claudeClient.ts` is needed — keep the interface in
  place so adding Gemini later doesn't require touching the translate route.
- `src/app/api/` — thin route handlers; core logic lives in `src/lib/`.
- Reader components go in `src/components/reader/`.

## Things to Be Careful About

- **Batching for translation**: when grouping paragraphs up to `maxChars`, never
  split a paragraph mid-way. If a single paragraph already exceeds `maxChars` on
  its own, send it whole anyway (forcing a split would break the sentence context).
- **Parsing the AI response**: the number of translated paragraphs returned must
  exactly match the number sent. If the AI returns a mismatched count (missing or
  extra separators), mark that batch as failed and do NOT update
  `lastTranslatedIndex` — better to retry than to corrupt or shift the data.
- **PDF parsing** (Phase 2) will be messy (headers/footers/page numbers get
  extracted along with the text). Don't assume it'll be clean on the first try —
  give the user a way to preview and clean up before committing to the DB.

## Common Commands

```bash
npx prisma migrate dev --name <name>   # after changing schema.prisma
npx prisma studio                       # inspect local data
npm run dev
```

## Definition of "Done" for a feature

Before considering a feature done, make sure:
1. It can be manually tested end-to-end from the UI (not just via API unit tests).
2. Progress (`lastTranslatedIndex` / `lastReadIndex`) updates correctly after the
   related action.
3. Refreshing the page / restarting the dev server does NOT lose progress
   (since this is the app's core value proposition — if this breaks, the app
   has failed its main purpose).